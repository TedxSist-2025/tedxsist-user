'use client'

import { Button } from "./button"

export function ClientButton() {
  return (
    <Button 
      variant="default" 
      className="select-none"
      type="button"
    >
      Register
    </Button>
  )
} 